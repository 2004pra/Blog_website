# Project Rules

## Command Execution
- **Never run commands directly.** Always present all commands (for testing, running, installing, etc.) to the user and let them run the commands themselves.
- If multiple commands are needed, list them all clearly in order.
